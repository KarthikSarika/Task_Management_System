<nav class="bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center h-16">
            <div class="flex items-center space-x-2">
                <i data-lucide="check-circle" class="w-8 h-8 text-indigo-600 dark:text-indigo-400"></i>
                <span class="text-xl font-bold text-gray-900 dark:text-white">TaskManager</span>
            </div>
            
            <div class="hidden md:flex items-center space-x-6">
                {% if user %}
                    <a href="/dashboard" class="text-gray-600 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium">Dashboard</a>
                    <a href="/tasks" class="text-gray-600 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium">Tasks</a>
                    <div class="flex items-center space-x-4 ml-4 pl-4 border-l border-gray-200 dark:border-gray-700">
                        <button id="dark-mode-toggle" class="p-2 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-full">
                            <i data-lucide="sun" class="w-5 h-5 hidden dark:block"></i>
                            <i data-lucide="moon" class="w-5 h-5 block dark:hidden"></i>
                        </button>
                        <div class="flex items-center space-x-2">
                            <i data-lucide="user" class="w-5 h-5 text-gray-400"></i>
                            <span class="text-gray-700 dark:text-gray-200">{{ user.name }}</span>
                        </div>
                        <a href="/auth/logout" class="text-red-600 dark:text-red-400 hover:text-red-700">
                            <i data-lucide="log-out" class="w-5 h-5"></i>
                        </a>
                    </div>
                {% else %}
                    <button id="dark-mode-toggle" class="p-2 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-full">
                        <i data-lucide="sun" class="w-5 h-5 hidden dark:block"></i>
                        <i data-lucide="moon" class="w-5 h-5 block dark:hidden"></i>
                    </button>
                    <a href="/auth/login" class="text-gray-600 dark:text-gray-300 hover:text-indigo-600 dark:hover:text-indigo-400 font-medium">Login</a>
                    <a href="/auth/register" class="bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition">Register</a>
                {% endif %}
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden">
                <button type="button" class="text-gray-500 hover:text-gray-600 focus:outline-none">
                    <i data-lucide="list" class="w-6 h-6"></i>
                </button>
            </div>
        </div>
    </div>
</nav>
